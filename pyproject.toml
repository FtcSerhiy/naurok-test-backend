[tool.poetry]
name = "naurok-test"
version = "0.2.0"
description = "This project get requests for naurok tests"
authors = ["Ftc Serhiy <ftcserhiy@gmail.com>"]
readme = "README.md"
packages = [
	{include = "cli"},
	{include = "server"},
	{include = "core"}
]
repository = "https://bitbucket.org/FtcSerhiy/naur.git"

[tool.poetry.dependencies]
python = "^3.10"
bs4 = "^0.0.1"
requests = "^2.28.1"
hug = "^2.6.1"
gunicorn = "^20.1.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"
autopep8 = "^2.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

